let arr = [1,3,5,7];

const cumulativeSum = (arr) => {
    let result = []
    let sum = 0
    for (let i = 0 ; i < arr.length; i++){
        sum = sum + arr[i]
        result.push(sum)
    }
    return result
}

console.log(cumulativeSum(arr))